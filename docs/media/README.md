# Media

Images used by the repository's README.

- `screenshot.png` — the library view, shown at the top of the README. Take it
  on a dark theme (the app's default) with a window wide enough that the track
  table is not cut off. Nothing here ships with the app.
